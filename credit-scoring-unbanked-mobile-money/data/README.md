# Data

The repository does not bundle sensitive mobile-money or credit records.

- `raw/`: downloaded raw datasets.
- `interim/`: intermediate cleaning outputs.
- `processed/`: modeling-ready datasets created by notebooks.
- `external/`: public metadata, schema templates, and feature dictionaries.

The main workflow downloads the UCI Default of Credit Card Clients dataset when Notebook 01 is run.
